/// <reference types="node" />

// Additional type declarations for the Angular RSVP application
declare global {
  interface Window {
    // Add any window extensions here if needed
  }
}

// Export empty object to make this a module
export {};
